import React, { FC } from 'react';
import WalletPageContainer from '../../../Containers/Pages/WalletPageContainer';

const Wallet: FC = () => {
    return <WalletPageContainer userType="students" />;
};

export default Wallet;
