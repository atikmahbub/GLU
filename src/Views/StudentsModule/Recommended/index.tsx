import React, { FC } from 'react';
import RecommendedPageContainer from '../../../Containers/Pages/RecommendedPageContainer';

const Recommended: FC = () => {
    return <RecommendedPageContainer userType="students" />;
};

export default Recommended;
