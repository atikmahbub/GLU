import React, { FC } from 'react';
import ViewProfilePageContainer from '../../../Containers/Pages/ViewProfilePageContainer';

const ViewProfile: FC = () => {
    return <ViewProfilePageContainer userType="students" />;
};

export default ViewProfile;
